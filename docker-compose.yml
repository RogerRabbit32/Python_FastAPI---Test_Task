version: "3.8"

services:
  app:
    build:
      context: .
    ports:
      - 8000:8000
    volumes:
      - ./app:/app
#    depends_on:
#      - db
#
#  db:
#    image: postgres:13
#    environment:
#      - POSTGRES_USER=kirill
#      - POSTGRES_PASSWORD=909961
#      - POSTGRES_DB=test_db
#    volumes:
#      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
